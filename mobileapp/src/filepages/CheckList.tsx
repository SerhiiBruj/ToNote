import React from 'react'
import { ScrollView, Text, View } from 'react-native'

const CheckList = () => {
    return (
        <ScrollView>
        <Text>
            Type in
        </Text>
    </ScrollView>

    )
}

export default CheckList
