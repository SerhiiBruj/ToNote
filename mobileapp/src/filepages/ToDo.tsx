import React from 'react'
import { ScrollView, Text, View } from 'react-native'

const ToDo = () => {
  return (
    <ScrollView>
    <Text>
        Type in
    </Text>
</ScrollView>
)
}

export default ToDo
